# Astra implementation status

Updated 6 September 2026. Current review worktree: `M:/gym-fidelity`, branch `review/mobbin-fidelity`, training-batch starting HEAD `3d0892c0143b84ccdb378566050a96b26a7bed06`. The sections below preserve earlier evidence from development branch `astra` and its original base `82b75bde5b4ac2fa355ee98e0efc39fc470ebac6`. The connected web implementation is not the completed production/native roadmap.

## Current checkpoint — training extension and hosted member persistence

Overall Future Pro reference scope remains **NOT COMPLETE / NOT VERIFIED 1:1**: 270 captures across 84 flows. Existing draft PR [#3](https://github.com/darkapoparka/gymaf/pull/3) targets `astra`; prior CI run `33999086780` passed web/database checks. The current training batch is uncommitted and has no new push or CI evidence at this checkpoint.

- `training-library.tsx` adds search, private favorites and a prescribed timed-work/rest filter over loaded assigned workouts. These are real assigned records, not public catalog/on-demand picks or estimated total durations. `exercise-history.tsx` reads completed-attempt actuals for the exact exercise identity and uses server `as_of` for period filters. `session-summary.tsx` renders saved Summary/Exercises states; `session.tsx` remains on the summary after completion. Location deletion in `member-views.tsx` now uses an in-app confirmation. No fabricated calories, media or health source was added.
- **Hosted member migration PASS:** following the owner's approval, source `20260905224715_member_frontend_records.sql` was installed on project `crhcgcqanoeoddmwaqhb` as `20260905235553` (`member_frontend_records`). Ignored `.env.local` was copied from Astra with publishable-key configuration and origin on port 3212. An existing authenticated session created `Temporary UI verification location` as Gym, selected Dumbbell, saved/reloaded, reopened Your Setup with count 1 and Dumbbell pressed, then deleted/reloaded to confirm absence. No other member data, health data or messages were created. This is a bounded persistence journey, not acceptance of all member flows or fresh email authentication.
- **Hosted training/conflict migrations PASS:** `20260906000529_training_library.sql` is installed as `20260906002504`; `20260906003017_application_conflict_codes.sql` is installed as `20260906003110`. The conflict migration changes only explicit `40001` raises to `GY409` in three named command functions, preserving uncaught genuine PostgreSQL serialization errors. Full migration chain, synthetic seed and core/member/training SQL suites passed on disposable PostgreSQL 17.4 databases `gymaf_training_final` and fresh `gymaf_training_conflicts` with simulated provider JWT context; the test cluster was stopped.
- **Hosted favorites browser evidence PASS, bounded scope:** save/reload retained the favorite pressed state and Favorites listing. Stale-tab attempts initially timed out twice because explicit application conflicts used retryable `40001`. After the migration, the stale tab immediately showed `CONFLICT`; Reload Favorites loaded the saved true state and cleared the alert. Unfavorite acknowledged restoration of the original false value, confirmed after reload; the temporary second tab was closed. This does not establish authenticated history/summary or wider cross-account behavior.
- **Local checks PASS, reported by the parent task:** 22 unit tests, expanded mocked-auth HTTP test including `GY409` to HTTP 409, and final full lint with zero errors/two pre-existing coach navigation warnings. Final production build including TypeScript and the foreground CSS correction passed; the mocked-auth HTTP test also passed after the build. The earlier `Date.now` purity defect and `npx` lint memory failure were resolved.
- **Bounded implementation review: SHIP; overall parity: FIX.** All reported correctable findings were resolved: markup, stale-error recovery, summary topology, history-sheet geometry and second-card contrast. Photographic media remains unresolved. `.artifacts/training-review/` contains the final `summary-393-fixed.png`, `summary-320-final.png`, `summary-1440-final.png`, `library-empty-320-fixed.png` and `player-history-393-fixed.png`, plus earlier valid library/favorites/exercises captures. At 393px the summary card is x=49.5/y=183.1875/294×522; the history sheet is x=0/y=136.328/width=393/bottom=852. Summary at 320px has no horizontal overflow. Exact evidence limits are in [FRONTEND_PARITY.md](FRONTEND_PARITY.md); detector `[]` is not a parity verdict. The latest inspected production design-review artifact is **404**. Authenticated history/summary and wider privacy checks, media, commit/push/CI and individual source-state acceptance remain open.

`DESIGN.md`, `PRODUCT.md` and `.impeccable` remain unchanged; the acknowledged stale legacy context was not approved for refresh. The current owner direction and [FRONTEND_PARITY.md](FRONTEND_PARITY.md) govern this parity task. Historical checks below retain their original scope and are superseded only where the current checkpoint explicitly records new evidence.

## Implemented code

| Area | Code delivered | Remaining acceptance |
|---|---|---|
| Identity | Provider email OTP, server-only HttpOnly cookies, explicit refresh/logout, app-session revocation, MFA enrollment/verification UI | Actual provider/browser/MFA recovery, multi-tab edge cases and abuse controls |
| Tenancy | Workspaces, assigned coach/client relationships, recipient-bound hashed invitations, RLS reads and operation-authorized commands | Wider role/reassignment/concurrency matrix and account suspension flows |
| Coach tools | Roster, draft builder, immutable publication, dated program assignment, client detail and check-in review | Full catalog/version workflow, UX refinement and larger-list pagination |
| Training | Distinct attempts, prescribed-versus-actual sets, resume after acknowledged saves, timestamp timing, completion/abandonment, history | Browser foreground/offline/conflict/device checks; rest guidance and complete original-player fidelity |
| Feedback | Weekly check-ins, authored coach reviews and durable text messages/read cursors | Browser-to-browser interaction checks, notifications UI/delivery and moderation operations |
| Service/account | Audited manual/complimentary entitlement backend, complimentary operator UI, cancel-through-end state, settings, support requests, bounded export | Real seller/payment model, recent-auth gates, automated deletion/retention, full lifecycle policies |
| Design | Retained CSS stack/tokens/layout vocabulary, separate connected screens and development-only legacy routes | Local visual approval, licensed assets/fonts, original branding and full BG localization |
| Test/CI | Twelve pure validation tests, PostgreSQL migrations/ownership/history/retry tests, local Supabase integration script, GitHub workflow | Local integration execution, browser/a11y/device suite and release/security review |

Runtime implementation is in `src/features/gymaf`, `src/shared/gymaf`, `src/server/gymaf`, explicit app/API routes and `supabase/`. Specifications remain in `/astra`. The old local store is not a production adapter and is not imported by connected domain components.

## Verified evidence

GitHub Actions run `33985694273` at source commit `5ef5cfd3a68f8f995d3ac44d3d9addb0e31ab405` completed successfully: clean npm install, the unit suite, ESLint, TypeScript, production build, migration application and database ownership/history regression tests. ESLint is passing with three internal-navigation warnings; it is not a zero-warning claim. The first run found a refresh-promise typing defect and render-time `Date.now` issue; both were fixed in that source commit and the checks rerun.

The database job uses real PostgreSQL with a **simulated provider-auth schema/JWT context**. It proves only the exercised migration/command/policy behavior. It does not prove Supabase Auth, email, HttpOnly cookie behavior, actual MFA, browser rendering, visual parity, load capacity or production security. The separate local integration script has been written but was not run in this environment.

The original container still cannot resolve GitHub for a full checkout; GitHub-hosted CI supplied the build/database execution above. Historical static audit reports are preserved at their original baseline and do not supersede these new, scoped results.

## Roadmap tracking

The original `backlog.json` remains the full release plan; no broad task is marked done simply because a first implementation exists. GY-001/002/004–019 have varying implementation coverage represented above. GY-003/008/021 have only partial content/localization/visual coverage. GY-020 and automated billing GY-025 are not implemented. Native GY-028–031 and discovery GY-032 are not implemented. Pilot/public release GY-024/026 remain blocked by technical, service and human gates.

A local agent should reconcile each acceptance criterion with current code and attach evidence, not restart all implemented features from scratch and not mark the entire backlog complete. Read `ADR-007-CONNECTED-WEB.md` for the changed implementation choices.

## Explicitly unfinished

No native iOS/Android binary, store billing, live Stripe integration, uploaded private photos/video pipeline, reviewed exercise-media catalog, generated brand pack, automatic data-erasure job, external notification outbox, full email/notification operations, complete Bulgarian translation, complete legacy-feature migration, public marketplace, physical-device verification, accessibility certification, production deployment or launch approval is delivered here.

Additional hardening includes recent-authentication enforcement for data export/deletion, authentication abuse limits/CAPTCHA, large-account pagination/export jobs, simultaneous permission-revocation/write races, stricter per-operation database payload validation, owner/staff recovery and suspension, final DTO minimization, browser refresh/retry error handling, and verified recovery/retention procedures. Do not use real customers to test these unfinished controls.

## Next local action

Follow `LOCAL_TESTING.md`, run the supplied checks against isolated synthetic Supabase, then exercise the real browser coach/client workflow and compare the interface. Fix failures in small commits on a local review branch. Keep `main` and live services untouched until the owner reviews the results.

## Local comparison inspection — 5 September 2026

Reviewed source `88cef03ca0b8c00ec3e3c4a5dba09daeb5023506` on local branch `astra-local-test`, tracking `origin/astra`, in the separate worktree `M:\gym-astra`. Original checkout `M:\gym` remains on local main `97b24278bdc70f2e1f2cba373acfcd6c56e0664d`; fetch updated remote refs but did not update local main. Its existing server remains at `http://127.0.0.1:3210`. Astra runs at `http://127.0.0.1:3211` with process environment `APP_ORIGIN=http://127.0.0.1:3211`. No application source or styling was changed during this inspection.

On Windows with Node `v24.18.0`: `npm ci` passed (372 audited packages, zero reported vulnerabilities); `npm run test:unit` passed 12/12; `npm run lint` passed with the three previously documented navigation warnings; `npm run typecheck` and `npm run build` passed. Unit execution reported Node's module-type warning. npm also reported an unapproved optional `unrs-resolver` install script; no blanket script approval was applied.

Backend setup is BLOCKED: Docker context is `desktop-linux`, but `docker info` did not respond, a bounded Docker server-version probe timed out after 15 seconds, and `npx --yes supabase start` timed out after 25 seconds. No Supabase listeners appeared on 54321–54324. The hung diagnostic/start processes launched for this inspection were stopped; Docker Desktop and other project runtimes were not restarted. The Supabase CLI also warned that `[inbucket]` is deprecated in favor of `[local_smtp]`. No database reset, seed, real-data access or integration suite was run.

Browser evidence: isolated `agent-browser` sessions rendered main's original home and Astra's landing/login. Astra's landing-to-login link worked, with no uncaught page errors reported. Submitting `coach-a@gymaf.example` displayed the expected missing Supabase configuration alert; it did not authenticate or send an email. Landing screenshots were inspected at 1440x1000, 393x852 and 320x800; the 393px login was also inspected. Local screenshots are under `.artifacts/local-review/` (ignored by Git).

Observed visual defect: the landing card labeled “Made for real coaching” clips its description horizontally at all three reviewed widths. This remains open; visual parity is not accepted. Authenticated layouts, publication/assignment, per-set persistence, repeat attempts/history, check-ins, messages, account switching, wrong-coach/sibling-client denial, MFA, real device behavior and direct integration tests are NOT TESTED because local backend setup is blocked.

Resume by recovering the local Docker engine, then verifying the `gymaf-astra` project has only disposable test data before any reset. For simultaneous comparison, align Supabase auth URLs and the seeded `.env.local` APP_ORIGIN to `http://127.0.0.1:3211` (the supplied defaults are 3210). Start Astra with `npm run dev -- --port 3211`; leave main on 3210. Complete seeding, integration checks and the browser journey before claiming connected workflow acceptance. No merge, public deployment, live payments or PR publication occurred.

## Hosted development connection — 6 September 2026

The owner subsequently authorized connecting the existing hosted `gymaf` project (`crhcgcqanoeoddmwaqhb`) through the now-working Supabase plugin. All four source migrations are installed, all 16 public application tables have RLS, and the local MFA bypass remains false. Astra's ignored `.env.local` uses an enabled publishable key and the 3211 origin; main remains unchanged on 3210. Local Docker recovery is no longer a prerequisite for this hosted connection, but the supplied local seed/integration scripts remain local-only.

Live Auth/REST connectivity, public-coach reads, anonymous denials and cross-origin rejection passed. The browser email request was accepted and advanced to code entry. The owner received and clicked the confirmation email, and a database read subsequently verified that the account is email-confirmed. The email contained a confirmation link; Astra's OTP verification and application session remain pending. The supplied dashboard login and existing shell management credential do not have access to this project's Auth settings, despite the plugin having database access. Five SECURITY DEFINER advisor warnings remain documented review items. Full details, migration-history mapping, restart commands and hosted email setup are in [HOSTED_TESTING.md](HOSTED_TESTING.md). No application source/styling, main branch, public deployment or live billing was changed.


## Hosted email-link repair — 6 September 2026

Root cause: hosted default emails contained links while Astra only accepted numeric OTPs, and the provider Site URL still pointed at unused localhost:3000. Correct dashboard access was established with Continue with ChatGPT. The URL is now 127.0.0.1:3211 with an exact /auth/callback allowlist. Free hosted template editing is locked without SMTP/Pro, so the review branch now supports server-only S256 PKCE email links via GYMAF_EMAIL_AUTH_MODE=link. Local code mode remains supported. No styling changes were made.

Acceptance: unit 15/15; lint 0 errors/3 existing warnings; typecheck/build pass. The built-app HTTP fixture passes request/challenge, callback exchange, verified user, application-session registration, cookie flags, verifier clearing and denial boundaries. Live malformed/missing callback and cross-origin checks pass. Browser renders the correct link form. A real fresh email request was rejected by the provider's locked 2-emails/hour limit, so hosted sign-in completion is BLOCKED on email delivery; no authenticated workflow acceptance is claimed. See HOSTED_TESTING.md for current evidence and restart steps. Main is unchanged on 3210, Astra runs on 3211. The earlier dashboard-access blocker is resolved; local Docker and the wider workflow/visual defects remain as recorded above.

## Private member frontend extension — 6 September 2026

Current work is in `M:/gym-fidelity`, review branch `review/mobbin-fidelity`, with source inspection at `df577e97a094a6f76658e7da137250097ed857f5` plus the uncommitted member batch. Earlier sections retain their historical branch, CI and provider evidence; they do not establish acceptance for these new changes. The current owner direction is the Future Pro Mobbin inventory of 270 captures across 84 flows, retaining Gymaf/Astra authentication, server commands and persistence. This extension preserves that pinned visual world; it does not replace `DESIGN.md` or its sidecar or repair the broader acknowledged legacy documentation drift.

Added connected private member records for app/workout preferences, locations and equipment, injury/limitation and movement exclusions, travel/events with details, weight measurements and a target. `member-area.tsx` owns loading/mutations and `member-views.tsx` owns presentation. Weight supports metric/imperial display with kilograms stored. Shared defaults in `src/shared/gymaf/member-preferences.ts` also feed the player, connecting instruction visibility, countdown, browser audio tone and supported vibration. Public profile sharing remains disabled. Injuries/exclusions and travel/events do not automatically alter a prescribed workout, notify a coach or imply coach acknowledgement.

The new `20260905224715_member_frontend_records.sql` migration adds `member_records`, owner-only RLS reads and validated `member.save`/`member.delete` commands with revisions and actor-scoped idempotency. Ownership comes from the active application actor; caller ownership overrides are rejected. There is no implicit coach sharing. Preferences and weight-target are singleton kinds, and preferences deletion is intentionally unsupported. [ADR-008-MEMBER-RECORDS.md](ADR-008-MEMBER-RECORDS.md) records the backend contract and deployment boundary. These domains are no longer wholly absent in source. The subsequently approved hosted installation and bounded location/equipment browser acceptance are recorded in the current checkpoint; wider member acceptance remains open.

Evidence for this batch, reported by the parent task unless explicitly identified as artifact inspection:

- Final production build including TypeScript after the last guard fix: **PASS**. Unit suite: **21 PASS**. Mocked-auth HTTP test: **1 PASS**, run sequentially after the build and superseding the earlier premature startup failure. Final lint: **0 errors, 2 pre-existing coach navigation warnings**. `git diff --check`: **PASS**. This mocked auth contract does not establish hosted email/authentication.
- Isolated PostgreSQL **17.4**, `.artifacts/member-db/data`, loopback port **55445**, clean disposable database **gymaf_member_final**: all migrations, synthetic seed, core coaching SQL tests and member SQL tests **PASS**. Coverage includes each member kind's supported CRUD/deletion rules, weight retry/revision, invalid/null inputs, ownership override and sibling/coach/anonymous denials. The provider schema/JWT context is simulated; this does not prove hosted Supabase Auth, cookies or a connected browser workflow.
- Final member review: **all six reported findings resolved** within scope—nested draft loss, equipment hierarchy, contrast/44px weight targets, 95px event-details textarea, in-flight weight locking and shared defaults. Equipment uses three columns, Your Setup/All Equipment, search and category filters; the reviewed 393px columns measured 115px. The event date group began at approximately y=404.59. Reviewed states had no horizontal overflow at 320/393/1440px. Header Back during a fixture tone save stayed on the view, and returning after save retained the tone. No geometry changed after the fixed captures. This is bounded responsive/fixture evidence, not a full route/source matrix.
- Development-only member fixture views cover settings, location/equipment, injury, event, weight/entry and app/workout preferences. They save synthetic in-memory state after 1200ms and make no API mutations. The current `.next/server/app/design-review.meta` was inspected and records **404**, confirming production-build exclusion. Local captures are under `.artifacts/member-review/`, including the six `*-fixed.jpg` captures and prior injury/location/settings captures listed in [FRONTEND_PARITY.md](FRONTEND_PARITY.md).

At this earlier member implementation checkpoint, hosted installation and review configuration were pending and no real client data was used. Those installation/configuration blockers were subsequently resolved under owner approval, with the bounded location/equipment browser journey recorded above. The local source/database/fixture results in this section retain their original scope; they do not establish production acceptance or cover the later training batch.

Overall status: **NOT COMPLETE / NOT VERIFIED 1:1** for all 270 captures and 84 flows. The member checkpoint mapped 214 captures to `Partial family adaptation` and 56 to `Not implemented`; these historical counts are not completed-screen counts. Continue from [FRONTEND_PARITY.md](FRONTEND_PARITY.md) and the current `connected-screen-parity.json`; pair each source state with its connected state and separately prove persistence, errors, conflicts, privacy, provider behavior and device capabilities. Local database tests and fixture screenshots do not substitute for those remaining gates.

## Review branch publication

Member source committed as `d64a56d` and pushed to `origin/review/mobbin-fidelity`. Draft PR [#3](https://github.com/darkapoparka/gymaf/pull/3) targets `astra`; no merge or application deployment occurred. The preceding working-tree descriptions identify the earlier inspection snapshot. Prior CI run `33999086780` subsequently passed web/database checks; this does not validate the current uncommitted training batch. The earlier member PostgreSQL test server was stopped after its successful checks; its synthetic data/logs remain under `.artifacts/member-db/`. The review app uses port 3212. Hosted member migration approval and installation are complete as recorded in the current checkpoint.

## Training review publication

Training source and verification were committed as `57fdaad7bb96529de3a248fe85312a9da334784d` and pushed to `origin/review/mobbin-fidelity`. Draft PR [#3](https://github.com/darkapoparka/gymaf/pull/3), “Connect Future Pro-style member and training flows”, still targets `astra`. GitHub Actions [34001688959](https://github.com/darkapoparka/gymaf/actions/runs/34001688959) was in progress at this publication checkpoint; its final result must be checked separately. The preceding “not yet” references describe their earlier inspection checkpoint. The worktree is connected to the existing hosted backend on port 3212; no main merge or production promotion was performed.
