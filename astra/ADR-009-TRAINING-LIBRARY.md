# ADR-009: Private workout bookmarks and exercise history

Accepted implementation scope, 6 September 2026. Extends ADR-007 and ADR-008 on `review/mobbin-fidelity`; no stack replacement.

The client can browse workouts already assigned to their selected coaching relationship, search titles/exercise names, filter prescribed timed work plus rest, and save private favorites. This does not publish private coach programs or create a public on-demand catalog. Timed targets are not estimates of whole workout duration. Existing `session.start` remains responsible for distinct genuine attempts, resumption, and training entitlement checks.

`workout_favorites` keys an account and an existing scheduled instance. RLS exposes only the active account's bookmarks. Direct writes and anonymous access are denied. `training.favorite` takes `scheduledId`, a boolean desired state, and a revision; it checks client ownership, serializes actor edits, preserves command idempotency, and logs the acknowledged change. Unfavoriting retains a false row and revision so a stale tab cannot silently overwrite a newer choice. At most 2,000 rows are accepted per account. No ownership fields come from the browser. Export includes the actor's favorites.

`GET relationships/[id]/favorites` and `GET workout-sessions/[id]/exercise-history/[exerciseId]` use authenticated server-side RPC calls. Exercise history inherits existing relationship/session/set RLS, requires the exact exercise identity from the requested prescription, and returns completed attempts with real set values. It never groups unrelated exercises by a similar name. The result contains a server `as_of` instant for stable range filtering and an explicit `has_more` flag beyond the 100 most recent matching attempts. Assigned coaches retain existing authorized history access; another coach and a sibling client are denied. Coaches cannot read the client's private favorites.

Completed or abandoned sessions now remain on their saved summary rather than immediately navigating away. Summary/exercise tabs display actual elapsed time and recorded sets; no calorie estimate, performed repetitions, feedback, or shared public link is fabricated. The summary's weekly check-in link opens the existing flow and sends nothing by itself. Exercise history is loaded only when its player sheet is opened.

## Evidence and boundaries

Migration `20260906000529_training_library.sql` was generated through the Supabase CLI. Full migrations plus synthetic seed and all three SQL suites passed in disposable PostgreSQL 17.4 database `gymaf_training_final`, port 55445. Provider/JWT context is simulated. Tests cover durable favorite/unfavorite, retry, revision conflict, malformed/owner override payloads, export isolation, direct-write denial, sibling and other-coach denial, completed-set history and assigned-coach history access. The test cluster was stopped afterward.

The separate, previously reviewed member migration is already installed on hosted project `crhcgcqanoeoddmwaqhb` as `20260905235553 member_frontend_records`. A temporary connected location with one Dumbbell selection was created, saved, reloaded, inspected, deleted and confirmed absent after refresh on port 3212. This proves only that narrow hosted member journey.

Training migration is installed on the hosted project as `20260906002504`. A real authenticated browser favorite/save/reload/unfavorite journey passed. A two-tab stale-revision test exposed provider timeouts, corrected by `20260906003017_application_conflict_codes.sql` (hosted version `20260906003110`). This changes only explicitly raised application conflicts in the three command functions from retryable SQLSTATE `40001` to `GY409`; genuine database serialization errors are not caught. The existing HTTP layer maps GY409 to 409. The corrected stale tab returns a conflict, Reload Favorites reads the current state and clears the error, and unfavorite succeeds afterward. [PostgREST issue 3673](https://github.com/PostgREST/postgrest/issues/3673) documents the relevant retry behavior; the diagnosis here was confirmed by the successful corrected browser journey.

The final full migration chain and all three SQL suites passed in `gymaf_training_conflicts`, with simulated JWT context. The HTTP fixture also verifies GY409 to HTTP 409. Final bounded visual review cleared the implementation corrections; media and total parity remain unresolved. Local presentation fixtures and screenshots are not proof of hosted persistence. Public picks/activity tracking, instructional media, exercise replacements/flags, per-session feedback, sharing, billing and native services remain outside this implementation. All source states remain NOT VERIFIED 1:1.
